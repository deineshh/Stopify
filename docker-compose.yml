services:
  spotifyclone.api:
    image: spotifyclone.api
    user: root
    build:
      context: .
      dockerfile: src/Apps/SpotifyClone.Api/Dockerfile
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_started
      redis:
        condition: service_started
    environment:
      DOTNET_RUNNING_IN_CONTAINER: true
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://+:8080
      Application__ApiUrl: http://spotifyclone.api:8080
      Jwt__SecretKey: some-fairly-super-super-long-secret-key-for-validating-JWTs
      Minio__Endpoint: minio
      Minio__LocalScratchPath: /app/wwwroot/temp/conversion
      Minio__AccessKey: admin
      Minio__SecretKey: password123
      MeiliSearch__Endpoint: http://meilisearch:7700
      MeiliSearch__MasterKey: meilikey
      ConnectionStrings__MainDb: Host=postgres;Port=5432;Database=spotifyclone_dev;Username=postgres;Password=postgres
      ConnectionStrings__Redis: redis:6379
    ports:
      - "5000:8080"
    volumes:
      - ./src/Modules/Streaming/SpotifyClone.Streaming.Infrastructure/Persistence/Initialization/AudioFiles:/app/seed_data/audio
      - audio_data:/app/wwwroot/audio
      - temp_data:/app/wwwroot/temp

  postgres:
    image: postgres:15
    container_name: spotifyclone-postgres
    environment:
      POSTGRES_DB: spotifyclone_dev
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio
    container_name: spotifyclone-minio
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: admin
      MINIO_ROOT_PASSWORD: password123
    command: server /data --console-address ":9001"
    volumes:
      - minio_data:/data
  
  redis:
    image: redis:alpine
    container_name: spotifyclone-redis
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    restart: always
    volumes:
      - redis_data:/data
  
  meilisearch:
    image: getmeili/meilisearch:latest
    container_name: meilisearch
    ports:
      - "7700:7700"
    environment:
      - MEILI_MASTER_KEY=meilikey
      - MEILI_ENV=development
    volumes:
      - meilisearch_data:/meili_data
    restart: always

volumes:
  audio_data:
  temp_data:
  postgres_data:
  minio_data:
  redis_data:
  meilisearch_data:
